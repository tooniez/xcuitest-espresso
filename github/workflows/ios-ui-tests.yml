name: iOS UI Tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: Load config
      run: cp .env.example .env
    - name: Run iOS tests
      run: ./scripts/run-ios.sh
